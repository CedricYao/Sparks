<?xml version="1.0"?>
<appSettings>
  <!--the framework pulls the database settings from the these web.config keys.  It looks for 
  any object that ends with "Settings" and structuremap will populate the object with the key value pair.-->
  <add key="DatabaseSettings.ConnectionString" value="Server=.\sqlexpress;Database=SparksSample;Trusted_Connection=True;"/>
</appSettings>